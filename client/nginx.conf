server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # For any URL that doesn't match a real file, serve index.html
    # This is required for Angular's client-side routing to work
    location / {
        try_files $uri $uri/ /index.html;
    }
}
